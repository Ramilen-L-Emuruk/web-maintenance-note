node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# tsc -b で vite.config.ts から生成される副産物（node_modules/.tmp へ出力するよう設定済み）
vite.config.js
vite.config.d.ts
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.playwright-mcp

# Claude Code が作るワークツリー・検証用スクリーンショット（Git 管理対象外）
.claude/worktrees/
.claude/screenshots/

# .claude/agents/ 配下はホワイトリスト方式:
#   既定で追跡せず、リポジトリ側で固定するものだけ ! で再包含する。
#   どれを固定するかの方針は CLAUDE.md「エージェント定義の置き場所」を参照。
#   追跡するファイルを増やすときは ! の行を必ず足すこと（追跡だけして
#   再包含を忘れると、一度 untracked に戻った時点で静かに追跡から漏れる）。
.claude/agents/*
!.claude/agents/adversarial-reviewer.md
!.claude/agents/doc-objectivity-reviewer.md
!.claude/agents/meta-reviewer.md
