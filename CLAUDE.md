# CLAUDE.md

web-maintenance-note（バイク メンテナンスノート）で作業するときの手順とルール。

## プロジェクト概要

- React 18 + TypeScript + Vite 5 製の PWA。バイクの給油・メンテナンス・保険情報を管理する。
- データ: Dexie（IndexedDB）によるローカル永続化。インポート/エクスポート機能あり。
- グラフ: Recharts を使用。
- GitHub Pages（`base: './'` + HashRouter でサブパスに依存しない配信）へ GitHub Actions で自動デプロイ。**`main` への push が `.github/workflows/deploy.yml` を起動し、そのまま本番へ反映される。**
- テスト: Vitest（`src/utils/fuel.test.ts` 等）。

## 変更時の基本フロー（必ずこの順で行う）

> **このフローは「ワークツリー内でコミットし、main に追随させ、ブランチを `ready/` へ改名する」（手順 11）ところで実質的に完了する**（手順 12 は後片付け）。
> main へのマージ・バージョン更新・プッシュは**このフローに含まれない**。複数セッションが並行して作業する場合に
> main が奪い合いになるため、main を書き換える操作はすべて下記「リリース」の手続きに集約している。
> **実装フローの途中では行わない**（理由は「リリース」節の「原則」を参照）。

1. **状況整理・修正方針の確認**（実装に着手する前に必ず行う。自明な軽微修正でも省略しない）
   - 対象のコード・挙動を調査し、現状と問題点（原因・影響範囲）を整理する
   - **被害の見積もりを、方針を提示する前に裏取りする。** 実装から確かめずに方針へ載せると、規模も優先度も対策の形も見当違いになる。とくに**並行処理・タイミングに関わる問題は、実測するか全経路を列挙する**まで断定しない（「同時に呼ばれる」と書く前に呼び出し元を全部数える、「こう壊れる」と書く前に壊してみる）
   - 整理した内容と修正方針（どこを・なぜ・どう直すか）をユーザーに提示する
   - ユーザーの確認を得てから次のステップに進む。指摘や方針変更があれば整理からやり直す
   - **複数の段・フェーズに分かれる作業は、着手前にタスクとして登録する**（下記「作業の記憶と切り出し」）
   - **ここで合意した範囲・深さ・粒度（「全て」等）を、後から自分の判断で狭めない。** 縮小が必要になったら実装の途中でも一旦手を止め、何をどこまで削るかを提示して確認を得る。**縮小した状態を先に成果物として出さない**
2. **ワークツリー作成**（修正・機能追加時はワークツリーを作成してから作業する。ブランチ名は必ず `worktree/<type>/<name>` 形式にする: `worktree/fix/〇〇`・`worktree/feat/〇〇`・`worktree/refactor/〇〇`・`worktree/docs/〇〇`・`worktree/chore/〇〇` など）
   - `worktree/` という接頭辞は**作業中**であることを表す。完了時に手順 11 で `ready/` へ付け替える
   - **Step 0 の `git pull` を飛ばさないこと。** ローカルの `main` が `origin/main` に遅れたまま切ると、後退した地点から作業を始めることになり、あとで作り直す手戻りになる
   - **注意**: `EnterWorktree(name: ...)` はブランチ名を自動変換してしまうため使用しない。必ず以下の手順で行う:
     ```bash
     # Step 0: メインの checkout 側でローカル main を最新へ
     git pull
     # Step 1: 正しいブランチ名でワークツリーを作成
     git worktree add -b worktree/<type>/<name> .claude/worktrees/<name>
     # Step 2: 作成したワークツリーに入る
     EnterWorktree(path: ".claude/worktrees/<name>")
     ```
3. **実装**（**ワークツリー内では `package.json` の `version` フィールドを変更しない**。バージョンはリリース時に一括で決める。理由は下記「リリース」参照）
4. **検証**（下記「検証」を実施。型チェック・テスト必須＋**実行確認（ブラウザ確認）必須**）
   - **本番ビルド確認（`npm run build` → `npm run preview` でブラウザ確認）まで行った場合は、その旨をコミットメッセージに記録する。** リリース時に検証範囲を判断する材料になる
5. **敵対的レビュー**（下記「敵対的レビュー」の手順で必ず実施。**全変更が対象。小規模修正でも省略しない**。指摘があれば実装に戻る。**ただし無条件に繰り返さない**——打ち切り条件は下記「巡回をどこで打ち切るか」）
6. **ドキュメント更新**（下記「ドキュメント更新」の条件に該当する場合。README・CLAUDE.md・コード内コメントを**同一コミットで**更新する）
7. **ドキュメント客観レビュー**（下記「ドキュメント客観レビュー」の手順で必ず実施。**ドキュメント、または利用者向けの文字列（UI 文言・データファイルの `description` 等）を更新した場合**。指摘があれば該当箇所に戻る）
8. **コミット前確認**（実装・検証・敵対的レビュー・ドキュメント更新が終わったら、コミットする前に必ずユーザーに確認を取る。**省略しない**）
   - 何を・なぜ・どう直したかを簡潔に提示し、コミットしてよいか確認する
   - 確認が取れるまで次のステップに進まない
9. **コミット**（確認が取れたら、下記「コミット」の規約に従いコミットする。バージョンはまだ変更しない）
10. **main の取り込み**（コミット完了後に必ず実施する。**省略しない**）
    - ワークツリー内で `git merge main` を実行し、ローカル `main` の先行分を取り込む
      - ここで取り込むのは**ローカルの `main`** でよい。`origin/main` がさらに先行していることもあるが、**その差はリリース手続きの着手前チェックで解消されるため、実装セッションが追いかけない**（`main` を進める操作をしないこと）
    - コンフリクトはここで解消する。解消したら型チェック・テスト・ブラウザ確認を再実行する。**解消が実質的なコード変更を伴った場合は敵対的レビューをもう 1 度回す**（どちら側のハンクを採るかの判断はバグを生みうるため）
    - コンフリクトを解消した場合は**マージコミットのメッセージにその旨を明記する**。リリース時の検証範囲の判断材料になる
    - **理由**: リリースまで待つ間にも main は他セッションのマージで進む。取り込みを後回しにすると乖離が積み上がり、リリース時にまとめて衝突する
11. **リリース待ちへの登録**（ブランチ名の接頭辞を `ready/` に付け替える。**省略するとそのブランチは永久にリリースされない**）
    - **改名前に必ずユーザーに確認を取る。省略しない**。確認は **AskUserQuestion** で次の 4 択を提示する
      - **ラベルは短く保ち、動作の詳細は選択肢の説明文へ回す。** 一覧を一瞥して 1 と 2 の違いが分かることが大事

      | # | ラベル | 動作 |
      |---|---|---|
      | 1 | リリース待ちのみ | 改名だけ行い、ワークツリーは残す |
      | 2 | リリース待ち＋片付け | 改名後、ワークツリーのディレクトリも削除する（**ブランチは残す**。手順 12 参照） |
      | 3 | 続けてリリース | 改名後、そのまま「リリース」の手続きへ進む |
      | 4 | まだ改名しない | `worktree/*` のまま留める（追加で直す予定がある場合）。待ち行列に現れないため手順 12 へは進まず、後日あらためてこの手順に戻る |

    - **確認の出し方**は状況で決める

      | 状況 | 出し方 |
      |---|---|
      | 発言がどの選択肢か一意に指している（「リリース待ちに入れて」＝1。ワークツリーの片付けまで求めていれば 2。「リリースして」＝3、改名前の「まだ直したい」＝4） | 4 択の提示を省略してそれを実行する。改名の可否はその発言で確認が取れている |
      | 発言から一意に決まらず（「コミットして」等）、**自分（Claude）が作業を完了したと判断できない**（追加のフェーズ・修正が続く見込みがある／完了か継続か判断がつかない） | **4 択を出さない。** 手順 9〜10 だけで止め、`ready/` の話は持ち出さない。コミットのたびに聞き直さない |
      | 発言から一意に決まらないが、**自分（Claude）が作業を完了したと判断できる** | 4 択を出す。**判断の主体は自分であって、ユーザーが完了を明言したかどうかではない。** ユーザーの意向を先読みして「まだ直したいかもしれないから」と自主的に省略しない——聞くコストは低く、続きがあるならユーザーが選択肢 4 を選べば済む |
      | 手順 10 でコンフリクト解消を伴った、または**伴ったか判然としない** | 発言が選択肢を指していても省略せず、**差分を提示してから**確認する |

      - コンフリクト解消の有無は、このセッションで手順 10 を実行していなければ対象ブランチの最後のマージコミット（`git log --merges -1 <branch>`）のメッセージで判定する。手順 10 はコンフリクト解消時にその旨を明記する規定のため
    ```bash
    git branch -m worktree/<type>/<name> ready/<type>/<name>
    ```
    - **`worktree/*` = 作業中、`ready/*` = 完成・リリース待ち。** この改名だけが「完了」の印になる
    - **理由**: 未マージのブランチには「実装途中で中間コミットしただけのもの」と「完成したもの」が混在し、コミット履歴や更新日時からは区別できない。名前で分けておかないと、リリース時に未完成のブランチを本番へ出しうる
    - ワークツリーのディレクトリ名（`.claude/worktrees/<name>`）は変わらない。リネームしてもワークツリーはそのまま使える
    - 同名の `ready/<type>/<name>` が既にあるとリネームは失敗する（過去のリリース中断などで残っている場合）。**勝手に別名を付けず**、状況をユーザーに報告して対象を確認する
    - **改名後に追加変更が必要になった場合**は、まず `git branch -m ready/<type>/<name> worktree/<type>/<name>` で作業中へ戻してから直す。検証・敵対的レビューを通し、あらためて `ready/` へ改名する。`ready/*` は「いつリリースされてもよい状態」を意味するため、そのまま触らない
      - 選択肢 2 でワークツリーを消していた場合は、**改名を戻した後に** `git worktree add .claude/worktrees/<name> worktree/<type>/<name>` → `EnterWorktree(path: ".claude/worktrees/<name>")` で作業を再開する。ブランチが残っているため巻き戻せる
    - **なぜ手順 8 のコミット前確認と分けるのか**
      - コミットの承認は「この変更を記録してよいか」であって「完成したか」ではない。改名は**この内容でリリースしてよい**という宣言であり、他セッションやリリース監視が候補として拾い得る状態にする**外向きの操作**
      - 手順 10 でコンフリクト解消を伴った場合は、コミット前確認で見せた内容と実際の中身が変わっている（差分提示が必要になるのはこのため）
12. **ここで実装セッションは終了**（選択肢 1・2 の場合。ブランチはリリース待ち行列に入る。選択肢 3 はそのまま下記「リリース」へ進む）
    - ユーザーに「コミット済み・リリース待ち」であることを伝える。**マージ・バージョン更新・プッシュを自発的に行わない**
    - **選択肢 2 を選んだ場合は、この場でワークツリーのディレクトリを削除する**
      - `ExitWorktree(action: "keep")` で抜けてから `git worktree remove .claude/worktrees/<name>`。**ブランチ（`ready/*`）は消さない**（消すとリリース候補の列挙に現れず、作業内容を指す ref も失われる）
      - リリース側はワークツリーが無ければ削除を飛ばし、ブランチの削除は通常どおり行う。マージ前に消しても手続きは成立する
      - 削除に失敗したら（未コミットの変更が残っている・別セッションやツールがそのパスを掴んでいる等）**`--force` で押し通さず**、状況をユーザーに提示して指示を仰ぐ
    - **選択肢 3 を選んだ場合**、またはユーザーがそのままリリースを指示した場合は、`ExitWorktree(action: "keep")` でワークツリーを抜けて「リリース」へ進む（出す候補が自分の 1 本だけなら、セッションを分ける必要はない）
    - セッションを終える際は、検証用に起動した dev サーバーを停止する（下記「検証」の「dev サーバーの扱い」参照）

### ユーザーからの合図

ユーザーが特別な言い回しを覚える必要はないが、次の意図は取り違えないこと。

| ユーザーの言葉 | 意味 |
|---|---|
| 「コミットして」 | 手順 9〜10 を実行する。`ready/` 改名を持ち出すかどうかは、手順 11 の「確認の出し方」の基準に従う |
| 「リリース待ちに入れて」 | 手順 11 の選択肢 1（ワークツリーも片付けるなら 2）を実行し、実装セッションを終える。**4 択の提示は省略してよい**（コンフリクト解消を伴った場合を除く。下記） |
| 「リリースして」 | 手順 11 の選択肢 3。続けて「リリース」の手続きへ進む。**4 択の提示は省略してよい**（同上） |
| 「これで十分」 | レビューの反復（敵対的レビュー・ドキュメント客観レビューのいずれも）を打ち切り、その時点の内容で確定する |
| 「まだ直したい」 | `ready/*` を `worktree/*` へ戻して作業を再開する（push 前ならいつでも巻き戻せる）。**改名前に言われた場合は選択肢 4** —— 改名しないだけで、巻き戻し操作は要らない |

**発言から選択肢が一意に決まっても、手順 10 でコンフリクト解消を伴った（または伴ったか判然としない）場合は 4 択を省略せず、差分を提示してから確認する。** 発言した時点と実際の中身が食い違っているため。

**push するまでは、いずれの操作も引き返せる。** コミットも `ready/` 改名もローカルの操作にすぎない。「もう戻れない」と誤解させないこと。

## 検証

> **コードを修正した場合は、型チェック・テストだけでなく必ずアプリを起動して実行確認（ブラウザ確認）まで行う。**
> 静的な確認だけで完了とせず、`npm run dev` で起動し Playwright MCP で実際の表示・挙動を確認してからコミットする。

- **型チェック（必須）**: `npx tsc -b`（または `npm run build`）。エラー0を確認する。
- **テスト（必須）**: `npm test`（Vitest）。既存テストを壊していないことを確認する。ロジックを足したらテストも足す。
- **アプリ起動**: `npm run dev` → `http://localhost:5173/`
  （`vite.config.ts` の `base: './'` により HashRouter で動作するため、サブパスは不要。5173 が使用中なら 5174 等にフォールバックするため、起動ログで実ポートを確認する）。
  - **必ず `run_in_background: true` でバックグラウンドタスクとして起動する**。フォアグラウンドで起動するとプロセスが応答を返さずハングするため。
- **ブラウザ確認（修正時は必須）**: Playwright MCP で上記 URL を開き、スクリーンショット・`browser_evaluate` で表示や DOM を確認する。
  - コンソールエラーが0件であることを確認する。
  - **スクリーンショットは `.claude/screenshots/` に保存する**（`.gitignore` 済み）。リポジトリ直下に出力すると誤コミットの元になる。
- **大きめの変更時は本番ビルドまで確認する**: `npm run build` → `npm run preview` でバンドル済みの成果物を起動し、ブラウザで動作確認する。
  - **理由**: dev サーバーは未バンドルでモジュールを解決するため、**本番ビルドでのみ壊れる不具合が dev では再現しない**（コード分割・minify・tree-shaking・アセットのパス解決に起因するもの）。dev だけで確認して preview を飛ばすと、デプロイ後に初めて発覚する事故が起きる。

> **`preview_start` / `preview_*` ツール（Claude Preview MCP）は使用しない。** ブラウザ確認は必ず Playwright MCP で行う。

### dev サーバーの扱い

- **検証用に起動した dev サーバーはセッション中は停止せず、次の検証で再利用する**（都度起動し直さない）。稼働中のサーバー（既定 5173）へ Playwright で接続する。
- **起動したサーバーは覚えておく。** 種別（dev / preview）・実ポート・**どのワークツリーで起動したか**を控える。再利用の規約が成り立つのはこれが分かっている間だけで、同じセッションで複数のワークツリーやバリアントを立てると、どれがどれか分からなくなる（記録の残し方は下記「作業の記憶と切り出し」）。
- **停止するときはポートから PID を引いて PID 指定で終了させる**（`netstat -ano` で PID を取り、`Stop-Process -Id <PID>`）。`Stop-Process -Name node` のような一括停止は MCP サーバー等の他プロセスを巻き込むため使わない。
  - バックグラウンドタスクとして起動した場合、タスクの停止だけでは子プロセスが残ることがある。残るとファイルを掴んだままになり、ワークツリーの削除が失敗する（Windows では `Permission denied` と出る）。PID 指定で確実に落とす。

## エージェント・スキルの置き場所

リポジトリ側に置いて Git 管理するのは次の 4 件だけ。

- `.claude/agents/` — `adversarial-reviewer` / `doc-objectivity-reviewer` / `meta-reviewer`
- `.claude/skills/` — `release`

**固定するかどうかは、工程の実行そのものを担うか、その成果物を検証するかで決める。** 走る頻度や規模条件では決めない。`adversarial-reviewer` と `doc-objectivity-reviewer` はレビューの実行そのもの、`meta-reviewer` は敵対的レビューが大規模構成で出した CRITICAL / HIGH を再検証する役（ドキュメント客観レビューには関与しない）、`release` はリリース工程の手順そのもの。`silent-failure-hunter`・`code-explorer` は既存の工程に観点を持ち込むだけなので固定しない。

固定する目的は、工程の中核が各マシンのユーザーレベル設定（`~/.claude/agents/`・`~/.claude/skills/`）の差で欠落しないようにすること。観点役（`silent-failure-hunter`・`code-explorer`）はユーザーレベル依存のままなので、中規模の敵対的レビューと大規模のドキュメント客観レビューは環境によって片方を欠いた状態で走りうる。

4 件はいずれも中身が現時点でユーザーレベルの同名ファイルと一致している。固定している以上、ユーザーレベル側を更新してもこちらへは伝わらない。揃えたいときは意識してコピーし直すこと。

同名のものをプロジェクト側に置くとユーザーレベル版を隠すので、上の 4 件以外はコピーしない。追跡対象の管理は `.gitignore` の該当節を参照。

ユーザーレベル版は検出観点としては使えるが、コード例が Java 中心のものがある（例: `silent-failure-hunter`）。例示はこのプロジェクト（TypeScript / React / Vitest / Dexie）に読み替えること。

## 敵対的レビュー

**全変更（コード・ドキュメント問わず）でエージェントによる敵対的レビューを実施する**。目的:
- 実装から裏取りしたバグ・エッジケース・機能間競合を検出する
- コミット前に「レビュー観点の抜け」を客観的に潰す
- 既存コメント・ドキュメントの誤解を疑う（実コードを信じる）

### 実施タイミング

**検証（型チェック・テスト・ブラウザ確認）が完了した後、ドキュメント更新の前**。指摘があれば実装に戻り、修正後に敵対的レビューをもう 1 度回す。
**ただし無条件に繰り返すわけではない**（下記「巡回をどこで打ち切るか」）。

### 巡回をどこで打ち切るか

**「指摘が出たら直してもう 1 巡」を無条件に続けると終わらない。** 修正が次の指摘を産む連鎖が起き、重要度も下がらないまま巡数だけが伸びる。**打ち切りの判断は Claude 側の義務**——ユーザーが「これで十分」と言うのを待つのは、収束していないことに気づく責任をユーザーへ押し付けている。

**「1 巡」= レビューを 1 回実行してから、指摘を反映して次のレビューを実行する直前まで。** 下記「エージェント構成」にある**並列のエージェントは何本でも合わせて 1 巡**。メタレビューも同じ巡に含める。

**CRITICAL は巡数によらず解消する。** CRITICAL が 4 巡目・5 巡目に出たなら、それは「継ぎ足しでは直らない」という兆候なので、**修正を重ねるのをやめて設計を見直す**——巡回を終える口実にはしない。打ち切ってよいのは HIGH 以下で、HIGH を残す場合は**残す判断をユーザーに確認する**（勝手に格下げしない）。

**比較するのは各巡の「最高重要度」だけ。** 件数の増減や MEDIUM / LOW の増減は判定に含めない（1 巡目 HIGH 2 件 → 2 巡目 HIGH 1 件＋MEDIUM 3 件は「下がっていない」）。曖昧な基準にすると「もう十分」と言いたい方向へ判断が寄る。

| 状況 | 判断 |
|---|---|
| 最高重要度が巡ごとに下がっている（HIGH → MEDIUM → LOW） | 収束している。そのまま続けてよい |
| **3 巡を超えても最高重要度が下がらない** | **修正を重ねるのをやめる。** 設計そのものを見直すか、残りを課題ボードへ積んで区切る（CRITICAL は上記のとおり打ち切れない） |
| 指摘が「前の巡で入れた修正の文面」だけになった | 実装の指摘が 1 件でも混じっているならまだ続ける。文面だけなら、まとめて直して締める |
| 同じ箇所を 3 巡以上書き換えている | その箇所の設計が問題。継ぎ足しをやめて作り直しを検討する |

打ち切るときは**何を残したかをユーザーへ明示し、課題ボードへ積む**。黙って終わらせない。「巡回を打ち切った」と「指摘が無くなった」は別の状態で、混ぜると次に触る人が誤解する。

### エージェント構成（変更規模で可変）

| 変更規模 | 例 | エージェント構成 |
|---|---|---|
| 小規模 | 1〜2 ファイルの typo/スタイル修正・軽微なパラメータ調整 | `adversarial-reviewer` 1 エージェント |
| 中規模 | 機能追加・複数ファイル・条件分岐の変更 | `adversarial-reviewer` + `silent-failure-hunter` の並列 2 エージェント |
| 大規模 | アーキテクチャ変更・複数機能横断 | 領域別に `adversarial-reviewer` を複数並列 ＋ **機能間競合レビュー**（機能 A × 機能 B の相互作用等）＋ **メタレビュー**（`meta-reviewer` で CRITICAL/HIGH の実装からの再検証） |

### レビュー観点（全エージェント共通）

- **実装から裏取り**: 既存コメント・README・CLAUDE.md を鵜呑みにせず、実コードを読んで挙動を確認する
- **修正はしない**: レビュアーは変更をコミットしない。指摘のみ返す
- **前の巡の修正を、元のコードと同じ厳しさで見る**: 指摘を潰すために足した抑制・ガード・早期 return が、**指摘の範囲より広い範囲を止めていないか**を確かめる。過剰補正は元の問題より重くなりうる
- **矛盾しているのは実装か文面かを見分ける**: 記録・通知の文面が事実以上を主張しているだけなら、**直すのは文面**。ロジックを止めて黙らせると、その検出機能ごと失う
- **分類**: CRITICAL / HIGH / MEDIUM / LOW で分類する
- **統一フォーマット**:
  ```
  [レベル] [ファイル:行] 見出し
  - 事実（実装からの引用）
  - 問題
  - 影響（どの機能・どの経路）
  - 修正方針の候補（実施はしない）
  ```

## ドキュメント更新

コードの変更に応じて、関連するドキュメントを**同一コミットで**更新する。対象は以下:

- **`README.md`**: 機能・画面構成・データモデル・依存・デプロイ方法・プロジェクト構成に影響する変更で最新化する
- **`CLAUDE.md`**: 変更時のワークフロー・検証手順・整合性チェックポイントに影響する変更のみ更新
- **コード内コメント**: 変更した処理のコメントを実装と一致させる（下記「コメント・ドキュメント整合性チェック」も参照）

スタイルの微調整・レイアウトの軽微な調整など、上記のいずれの記載にも影響しない変更ではドキュメント更新不要。

## ドキュメント客観レビュー

**ドキュメントを更新した場合、および利用者向けの文字列（UI 文言・データファイルの `description` 等）を新規作成・更新した場合**、対象箇所にエージェントで客観レビューを実施する。
目的: 技術的すぎないか・過剰でないか・初見の読み手に伝わるか・制作過程が漏れていないか・実装と乖離していないかを検証する。

**対象は「ドキュメント」に限らない。** 型定義が「概要を書く場所」と定めたフィールドに実装の作業経緯を書いてしまう事故は、仕様書や README と同じ穴で起きる（下記「データファイル・UI 文言特有の観点」）。

### 実施タイミング

**ドキュメント更新・該当文字列の作成/更新の直後、コミット前確認の前**。指摘があれば該当箇所に戻る。

### エージェント構成

- 更新規模が小さい場合（1 ファイル・数行の追記）: `doc-objectivity-reviewer` 1 エージェント
- 更新規模が大きい場合（README 大幅改訂・CLAUDE.md 追記）: `doc-objectivity-reviewer` + `code-explorer` の並列 2 エージェント（後者は「初見の開発者視点」で追加観点を担当）

### レビュー観点

- **読者が誰か明示されているか**: 一般利用者向け / 開発者向け 等
- **前提知識のない読み手が理解できるか**: 過度な略語・専門用語の初出説明はあるか
- **実装との乖離がないか**: パラメータ値・関数名・ファイル名の正確性
- **冗長でないか**: 同じ内容が複数箇所に重複していないか（重複は単一情報源へ集約する候補）
- **技術的すぎないか**: 「実装の内部詳細」と「読者が知るべき挙動」の粒度が分離されているか
- **過剰でないか / 情報量が適切か**: 実装の複雑度・機能の重要度に対して分量が釣り合っているか
- **制作過程が漏れていないか**: 「ご要望どおり」「前版と違い」「ご指定により X は使っていません」など、読者の行動ではなく**その文書がどう作られたか**を語る記述が残っていないか。制約は成り立ちとして説明せず、手順・既定値・用語選びそのものに反映する。読者に守らせる禁止指示（「X は使うな」）と、判断を再現するために置いた設計根拠（`理由:` `根拠:` の類）はこれに当たらない

### データファイル・UI 文言特有の観点

`description` 等のメタデータフィールドや UI 表示文言は「その機能を初めて触る利用者」向けで、ドキュメントよりさらに読者の予備知識を仮定できない（実値との一致は下記「コメント・ドキュメント整合性チェック」の担当。ここで見るのは伝わりやすさと作業経緯の漏れ）:
- 会話・作業経緯に由来する語（作業フェーズ名・スコープ縮小の弁明・「ご要望どおり」等）が残っていないか
- **フィールドの用途に沿っているか** — 型定義のコメントが「何を書く場所か」を定めているなら、それに従っているか
- 実装上の妥協・簡略化・データの網羅範囲（「◯件のみ収録」「概算」「代表値」等）は、利用者向けフィールドではなく開発者向けの記述側に書かれているか。**利用者向けフィールドに書いてよいのは、対象そのものを説明する事実だけ**

### 出力形式

```
[レベル] [ファイル:節/行] 見出し
- 記述の該当箇所
- 何が伝わりにくいか / 何が実装と食い違うか
- 修正方針の候補
```

レベルは CRITICAL（誤情報・法的リスク）/ HIGH（読み手が誤解する）/ MEDIUM（冗長・重複・制作過程の漏れ）/ LOW（表現改善余地）。

## プランモード

- プランモードに入ったとき、前回のプランが**完了済み**（実装・コミット済み）の場合は、既存プランファイルを修正せず**新規プランファイルを作成**する。
- 前回プランが未完了（作業途中）の場合のみ、既存プランファイルを引き続き更新してよい。

## 作業の記憶と切り出し

コンテキストは失われる。**会話へ書いただけの計画は、失われた時点で存在しなくなる。**
この節は「思い出せる形で残す」ための決まり。

> ここでいう**チップ**とは、`spawn_task` で作る「別セッションへの申し送り」のこと。
> ユーザーが 1 クリックで別セッションとして開始できる（扱いは下記「作業中に別の課題を見つけたとき」）。

### 進める作業はタスク機能で管理する

**複数の段・フェーズに分かれる作業は、着手前に `TaskCreate` で登録する。**

- **依存関係を宣言する**。`TaskUpdate` の `addBlockedBy` / `addBlocks` で「A が終わるまで B は着手できない」を表すと、`TaskList` が blocked として返す。順序を取り違えない
- 状態は `pending` → `in_progress` → `completed`。**着手時に `in_progress`、完了時に `completed`**。部分的にしか終わっていないものを `completed` にしない（テストが落ちている・実装が途中・必要なファイルが見つからない、のいずれも完了ではない）
- **単一で些末な作業には使わない**（`TaskCreate` 自身がそう定めている）。目安は段・フェーズに分かれるか、3 ステップ以上か
- **タスク機能やチップ（`spawn_task`）はハーネスが提供するもので、どの環境にもあるとは限らない。**使えない配布先では、この節が求める記録を資材（リポジトリ内のファイル）への直接記述だけで満たす
  - 進捗管理の道具は環境によって名前が違う（`TaskCreate` 系のほか、`TodoWrite` を持つ環境もある）。**ここで定めているのは道具ではなく記録の要件**——依存関係を宣言し、着手と完了を区別し、消えては困るものを資材へ残すこと。手元にある道具でこれを満たせばよい
- **消えては困る計画は資材にも書く。** タスクリストはセッション用で、永続する保証がない。手順・判断基準はリポジトリ内の資材（`CLAUDE.md`・仕様書配下）へ置く。Claude のメモリは端末ごとに閉じているため代わりにならない
- **検証用に起動したサーバーも記録の対象。** 種別（dev / preview）・実ポート・**どのワークツリーで起動したか**を `TaskCreate` の `metadata` へ書く（何を控えるかと理由は上記「dev サーバーの扱い」）

**会話へ書いただけの計画は、セッションが再起動した時点で自分でも思い出せなくなる。** ユーザーから見れば「決めたはずのことを忘れている」状態で、指摘されるまで気づけない。

### 作業中に別の課題を見つけたとき

**まず現在の変更と競合するかを判定する。** 競合するなら同じセッションで直すことを提案し、独立なら `spawn_task` でチップに切り出す。

競合と見なす条件（どちらかを満たせば競合）:

- **同じファイルを触る**（同じ関数である必要はない。対になる関数や近接する処理も含む）
- **前段の結果でその課題の方針が変わる**（直列の依存がある）

| 例 | 判定 | 扱い |
|---|---|---|
| いま直している関数と対になる関数に同じ欠陥がある | 同じファイル → 競合 | 同じセッションで直す |
| 別ファイルにあるコメント漏れ・命名の不統一 | 独立 | チップ |
| 前段の調査結果を受けて方針が決まる複数の段 | 直列依存 → 競合 | **1 本のチップにまとめる** |

**チップ同士が競合する場合は 1 本にまとめる。** 1 クリックで別セッションが始まるため、依存関係のあるものを並べると同時に押せてしまう。プロンプトに「前提」と書いても、**それを守る責任をユーザーへ押し付けるだけ**で、依存そのものは表現できない。依存を表現したいなら `TaskUpdate` の `addBlockedBy` を使う——チップにその機能は無い。

**自分でそのチップの内容に着手したら、着手した時点で `dismiss_task` で取り下げる**（完了時ではない）。残したままだとユーザーが押し、別セッションが同じ修正を並行して始める。

### 会話の内容を思い出せないときはトランスクリプトを読む

コンテキストが失われて「前に何を決めたか」が分からなくなったら、**推測で進めず記録を読む**。

```
<ホームディレクトリ>/.claude/projects/<プロジェクトのパスをハイフンで繋いだ名前>/<sessionId>.jsonl
```

- `sessionId` は環境変数 `CLAUDE_CODE_SESSION_ID` から取れる。**まずこれを見ること**——追加のツールを必要としない
  - `mcp__ccd_session_mgmt__get_session` に `"self"` を渡すとタイトルも併せて分かるが、この MCP が入っていない環境では使えない
  - **どちらも使えないときは、対象プロジェクトのディレクトリにある `.jsonl` を更新時刻の新しい順に見て候補を絞る。** `sessionId` が分からないことは、読む手段が無いことを意味しない
  - **ただし「一番新しい」は「一番最近動いた」であって「自分」ではない。** 同じプロジェクトで
    別セッションが並行していれば、そちらを掴む。**採用する前に、中身が自分の文脈と合うかを
    確かめること**（作業ディレクトリ・直近に触ったブランチ名・時刻の連なり）。
    決め手が無ければ要点をユーザーへ提示して確認を取る。他人の会話を「自分が決めたこと」として
    取り込むと、以後の判断がまるごとずれる
- JSONL 形式（1 行 1 メッセージ）なので grep できる。再起動前の分が `.jsonl.pre-import` に残ることもある
- **`list_events` と `search_session_transcripts` は自分のセッションを対象にできない。** 自分の記録はファイルを直接読むしかない。ここはハーネス側の仕様なので、動かなくなったらまず挙動が変わっていないかを疑うこと

**「復元できない」と決めつけて調査をやり直さないこと。** 記録は最初からファイルとして存在する。「無い」と判断する前に、読む手段があるかを確かめる。

## コミット

- **Conventional Commits**（`feat` / `fix` / `refactor` / `docs` / `chore` / `perf` / `ci`）。説明は日本語。
- コミットメッセージ末尾に必ず付与する。**モデル名はその時作業している Claude モデルに合わせる**（ハーネスが指定するモデル名を使用する。特定モデルに固定しない）:
  ```
  Co-Authored-By: Claude <モデル名> <noreply@anthropic.com>
  ```
- **コミット前に必ずユーザーに確認を取る。省略しない**。同一セッション内で別の修正のコミット許可をもらっていても、その修正のコミット許可を個別にもらっていない場合は確認する。
- ワークツリー内のコミットでは `package.json` の `version` を変更しない（下記「リリース」参照）。バージョン確定はリリース時にまとめて行う。
- **`git status` に意図しない `*.png` が現れていないか確認する。** `.claude/screenshots/` 以外へ出力されたスクリーンショットは `.gitignore` に掛からない。
- Windows 環境のため `LF will be replaced by CRLF` の警告が出るが正常（無視してよい）。

## リリース

main を書き換える唯一の手続き。**具体的な手順は `release` スキル（`.claude/skills/release/SKILL.md`）に集約している。**

- `/release` と入力するか「リリースして」と指示してスキルを起動する。**ワークツリー内にいる場合は `ExitWorktree(action: "keep")` で抜けてから起動する**（ワークツリー内からは main を操作できない）。
- 出す候補が自分の 1 本だけなら、実装したセッションでそのまま続けてよい。リリース待ちが複数溜まっているときは、リリース専用のセッションを立てた方が実装の文脈と判断を混同しにくい。
- スキルは起動時に `ready/*` を探し、あればリリース手順へ進む。無ければ**ブランチ監視モード**に入り、`ready/*` が現れた時点でユーザーに確認する。
- 監視中に検知しても、**ユーザーの承認なしに main を触ることはない**。

### 原則

- **main を書き換える操作（マージ・バージョン更新・プッシュ・マージ済みブランチの削除）は、リリース手続きの中でのみ行う。** 実装フローの途中では行わない。
  - ワークツリー内から main は操作できない（ワークツリー内での `git merge` は main ではなく**自分のブランチに黙って適用される**）。実装フローの途中で試みても正しく動かない。
- **ファストフォワード可能な場合でも、必ずマージコミットを作成する（`git merge --no-ff`）。** `--ff-only` や暗黙のファストフォワードでマージしない。各機能・修正の単位を履歴上で明確に残し、後から変更のまとまりや取り消し範囲を追いやすくするため。
- **リリース手続きを同時に 2 つ走らせない。** 並行すると、両方が同じバージョン番号・同じタグを採番して push が衝突する。着手前に `.claude/release.lock` の有無で確認するが、**ファイルの確認と作成は不可分ではない**ため完全な排他ではない。技術的な最後の砦は push 直前の `git fetch` 再確認と non-fast-forward での拒否（詳細はスキル側の「リリースロック」）。
- **1 回のリリースで複数ブランチをまとめて出してよい。** バージョンはリリース 1 回につき 1 回だけ上げる（機能 1 本ごとではない）。
- **`package.json` の `version` を書き換えてよいのはリリース手続きの中だけ。** ワークツリー内やマージ前のブランチでは変更しない。
  - 理由: 複数のワークツリーを並行して進めていると、各セッションがワークツリー作成時点の古いバージョンを見て同じ番号を選んでしまい、正しく積み上がらない（例: 1.0.0 から 2 セッションが同時にパッチ修正すると、両方とも 1.0.1 を選んで最終的に 1.0.1 のまま残る）。リリースを 1 セッションに限れば、常に最新の値を見て +1 できる。
  - 更新には `npm version patch|minor|major` を使う（ファイル更新・コミット作成・タグ作成まで自動で行う）。
- **push は GitHub Actions のデプロイを起動する。** 統合後の検証（本番ビルド + `npm run preview` でのブラウザ確認）を通してから push する。
- プッシュ時はタグも一緒に送る: `git push --follow-tags`

## TypeScript の型付け

- **公開 API には明示的な型を付ける** — export する関数の引数と戻り値。ローカル変数は推論に任せる。同じインラインのオブジェクト形が繰り返し現れたら名前付きの型に切り出す。
- **`interface` と `type` を使い分ける** — オブジェクトの形は `interface`、ユニオン・交差・タプル・マップ型・別名は `type`。`enum` は使わず文字列リテラルのユニオンにする。
- **`any` を使わない** — 外部から来る値・信頼できない入力は `unknown` で受けてから絞り込む（`error instanceof Error` 等）。呼び出し側で型が決まるなら generics にする。
  - **例外**: TypeScript の標準型定義に無い実験的な Web API。現状は `src/utils/notify.ts` の Periodic Background Sync だけ。使用箇所を最小限に閉じ込め、標準の型で書けない理由をコメントに残す（`notify.ts` が手本）。
- **コンポーネントの props は型注釈を省略しない** — 書き方は名前付きの `interface Props` とインラインのオブジェクト型（`{ data }: { data: FuelEconomyPoint[] }`）が混在している。どちらでもよく、既存を揃え直す必要はない。コールバックも型を明示する（`onClose: () => void`）。`React.FC` は使わない。

## コメント・ドキュメント整合性チェック

コードを変更した際は、以下の照合を必ず行う。変更が小さくても省略しない。

### 変更前後に確認すること
- **数値・閾値の一致**: コメントに書かれた件数・閾値・単位換算の係数が実コードの値と一致するか。
- **列挙の網羅性**: コメントが列挙する要素（メンテナンス項目の種別・画面名など）が実装のすべての要素を網羅しているか。
- **UI 説明文と実データの照合**: 画面に表示する説明文・ラベルが、それが参照するデータ（Dexie のスキーマ定義・定数）の実値と一致するか。

### コメント腐敗を防ぐパターン
- 処理の「単位」「対象範囲」「条件」を書いたコメントは、実装変更時に必ず追従させる。
- 「A または B」「A の場合のみ」のような条件を述べるコメントは、実コードの条件式と突き合わせて確認する。
- 古い識別子・旧実装の痕跡（コンポーネント名・関数名・テーブル名）がコメントに残っていないか確認する。

### README のプロジェクト構成ツリー
- `README.md` にプロジェクト構成ツリーがある場合、新ファイルを追加した際は必ず追記する。削除・リネームしたファイルも同様に更新する。

## 補助コマンド

| コマンド | 用途 |
|----------|------|
| `npm run dev` | 開発サーバー（`http://localhost:5173/`） |
| `npm run build` | 型チェック + 本番ビルド |
| `npm run preview` | 本番ビルドのプレビュー |
| `npm test` | テスト実行（Vitest・1 回だけ走らせて終了） |
| `npm run test:watch` | テストの監視実行 |

## 構成メモ

- `src/`: アプリケーションのソースコード。
- `vite.config.ts`: `base: './'` + HashRouter でサブパスに依存しない配信。GitHub Pages でも 404 にならない。
- `vite-plugin-pwa`: PWA マニフェストと Service Worker の自動生成。`registerType: 'autoUpdate'` で自動更新。
- データ永続化: Dexie（IndexedDB ラッパー）。`dexie-react-hooks` で React コンポーネントからリアクティブに参照。
- グラフ: Recharts（給油燃費グラフ等）。
- ルーティング: `react-router-dom` の HashRouter。
