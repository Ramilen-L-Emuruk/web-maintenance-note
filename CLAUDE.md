# CLAUDE.md

web-maintenance-note（バイク メンテナンスノート）で作業するときの手順とルール。

## プロジェクト概要

- React 18 + TypeScript + Vite 5 製の PWA。バイクの給油・メンテナンス・保険情報を管理する。
- データ: Dexie（IndexedDB）によるローカル永続化。インポート/エクスポート機能あり。
- グラフ: Recharts を使用。
- GitHub Pages（`base: './'` + HashRouter でサブパスに依存しない配信）へ GitHub Actions で自動デプロイ予定。

## 変更時の基本フロー（必ずこの順で行う）

1. **ワークツリー作成**（修正・機能追加時は必ず `EnterWorktree` でワークツリーに入ってから作業する。ワークツリー名にはコミット種別に合わせたプレフィックスを付ける: `fix/〇〇`・`feat/〇〇`・`refactor/〇〇`・`docs/〇〇`・`chore/〇〇` など）
2. **実装**
3. **検証**（下記「検証」を実施。型チェック必須＋**実行確認（ブラウザ確認）必須**）
4. **README 更新**（下記「README 更新」の条件に該当する場合）
5. **バージョン更新確認**（コミット前に **AskUserQuestion ツール**で必ず以下の選択肢をユーザーに提示する。**省略しない**）
   - 「メジャーバージョンを上げる」「マイナーバージョンを上げる」「パッチバージョンを上げる」「バージョンを上げない」
   - バージョンを上げる場合は `package.json` の `version` フィールドを更新してからコミットする
6. **コミット**（下記「コミット」の規約に従い、確認を求めず自動で行う）
7. **main へのマージ**（**ユーザーから明示的に指示があったときのみ**。ワークツリーの変更を main にマージする前に必ず確認する）
8. **プッシュ**（**ユーザーから明示的に指示があったときのみ**。自動では行わない）

## 検証

> **コードを修正した場合は、型チェックだけでなく必ずアプリを起動して実行確認（ブラウザ確認）まで行う。**
> 型チェックのみで完了とせず、`npm run dev` で起動し Playwright MCP で実際の表示・挙動を確認してからコミットする。

- **型チェック（必須）**: `npx tsc -b`（または `npm run build`）。エラー0を確認する。
- **アプリ起動**: `npm run dev` → `http://localhost:5173/`
  （`vite.config.ts` の `base: './'` により HashRouter で動作するため、サブパスは不要。5173 が使用中なら 5174 等にフォールバックするため、起動ログで実ポートを確認する）。
  - **必ず `run_in_background: true` でバックグラウンドタスクとして起動する**。フォアグラウンドで起動するとプロセスが応答を返さずハングするため。
  - **検証用に起動した dev サーバーは Claude のセッション中は停止せず起動したままにする**。次の検証では新規起動せず、稼働中のサーバー（既定 5173）へ Playwright で接続して再利用する。
- **本番ビルド確認**（大きめの変更時）: `npm run build`。
- **ブラウザ確認（修正時は必須）**: Playwright MCP で上記 URL を開き、スクリーンショット・`browser_evaluate` で表示や DOM を確認する。
  - コンソールエラーが0件であることを確認する。
  - 検証用スクリーンショットはリポジトリ直下に出力されるが**一時ファイル。コミット前に必ず削除する**（コミットしない）。

## README 更新

機能・画面構成・データモデル・依存・デプロイ方法・プロジェクト構成に影響する変更では、`README.md` の該当箇所も合わせて最新化する。
スタイルの微調整やレイアウトの軽微な調整など、README の記載に影響しない変更では更新不要。

## プランモード

- プランモードに入ったとき、前回のプランが**完了済み**（実装・コミット済み）の場合は、既存プランファイルを修正せず**新規プランファイルを作成**する。
- 前回プランが未完了（作業途中）の場合のみ、既存プランファイルを引き続き更新してよい。

## コミット

- **Conventional Commits**（`feat` / `fix` / `refactor` / `docs` / `chore` / `perf` / `ci`）。説明は日本語。
- コミットメッセージ末尾に必ず付与:
  ```
  Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
  ```
- 検証用スクリーンショット（`*.png`）はコミットに含めない。
- Windows 環境のため `LF will be replaced by CRLF` の警告が出るが正常（無視してよい）。

## 補助コマンド

| コマンド | 用途 |
|----------|------|
| `npm run dev` | 開発サーバー（`http://localhost:5173/`） |
| `npm run build` | 型チェック + 本番ビルド |
| `npm run preview` | 本番ビルドのプレビュー |

## 構成メモ

- `src/`: アプリケーションのソースコード。
- `vite.config.ts`: `base: './'` + HashRouter でサブパスに依存しない配信。GitHub Pages でも 404 にならない。
- `vite-plugin-pwa`: PWA マニフェストと Service Worker の自動生成。`registerType: 'autoUpdate'` で自動更新。
- データ永続化: Dexie（IndexedDB ラッパー）。`dexie-react-hooks` で React コンポーネントからリアクティブに参照。
- グラフ: Recharts（給油燃費グラフ等）。
- ルーティング: `react-router-dom` の HashRouter。
